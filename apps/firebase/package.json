{
  "name": "@jupiter/firebase",
  "packageManager": "yarn@3.2.4",
  "scripts": {
    "dev": "firebase emulators:start --import ./.emulator-data --export-on-exit",
    "test": "firebase emulators:start --help"
  },
  "devDependencies": {
    "firebase-tools": "^11.18.0"
  }
}
